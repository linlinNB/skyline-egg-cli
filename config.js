/**
 * 作用: 配置项目主要路径
 */
module.exports = {
  // 根目录
  root: __dirname,

  // 执行命令目录路径
  dir_root: process.cwd(),

  // 项目主要路径
  entry: './',

  // 项目编译输出路径
  output: './',

  // 配置模板目录
  template: __dirname + '/template'
};
